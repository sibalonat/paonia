<?php

/** @var \Illuminate\Database\Eloquent\Factory $factory */

use App\TagThread;
use Faker\Generator as Faker;

$factory->define(TagThread::class, function (Faker $faker) {
    return [
        //
    ];
});
